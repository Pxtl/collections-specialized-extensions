# Plan

- Test coverage

- Coding standards & enforcement

- Support System.Collections.ObjectModel (done), System.Collections.Specialized (done), System.Collections.Concurrent (TODO?), System.ComponentModel collections (done).

- Also a pure Interface `I*` one that doesn't take the type parameter and just returns the raw interface, but still takes the `Func<T>`?

- NameObjectCollectionBase (similar to IDictionary).  For example, HTTPClient CookieCollection could use this.